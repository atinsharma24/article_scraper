# Architecture diagram (to paste into README)

Create a simple box-and-arrow diagram showing:
- React (Vercel) -> Laravel API (Render) -> Supabase Postgres
- Node Cron (Render) -> Laravel API (Render)
- Node Cron (Render) -> SERP API (SerpAPI)
- Node Cron (Render) -> External websites (scrape)
- Node Cron (Render) -> LLM API
